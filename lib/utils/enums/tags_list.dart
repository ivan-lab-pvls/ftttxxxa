import 'package:restaurantdelapp/utils/models/tag_model.dart';

class TagsList {
  static const List<TagModel> tagsList = [
    TagModel(title: 'SOUPS'),
    TagModel(title: 'ROLLS AND SETS'),
    TagModel(title: 'SNACKS AND SALADS'),
    TagModel(title: 'NOODLES / RICE'),
    TagModel(title: 'POULTRY DISHES'),
  ];
}
